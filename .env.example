#############################################
# n8n — Production-Ready Environment Configuration
#############################################

# Basic Configuration
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http

# Security (IMPORTANT: Generate a strong encryption key)
N8N_ENCRYPTION_KEY=your-super-secure-encryption-key-here-change-this

# Database Configuration (PostgreSQL)
DB_TYPE=postgresdb
POSTGRES_DB=n8n
POSTGRES_USER=n8n
POSTGRES_PASSWORD=n8n_password

#############################################
# Community Nodes
#############################################

# Enable installing community nodes
N8N_COMMUNITY_PACKAGES_ENABLED=true

# Allow unverified community packages
N8N_UNVERIFIED_PACKAGES_ENABLED=true
N8N_VERIFIED_PACKAGES_ENABLED=true

#############################################
# Executions and History
#############################################

# Delete old execution data automatically (default true)
EXECUTIONS_DATA_PRUNE=true

# Keep executions for X hours (default 336h = 14 days)
EXECUTIONS_DATA_MAX_AGE=336

#############################################
# Webhook & REST Endpoints (Optional)
#############################################

# URL where users access the editor (public URL of your n8n instance)
# Recommended for production/when using webhooks.
# N8N_EDITOR_BASE_URL=https://your-n8n.example.com

# The base URL n8n uses to create webhooks.
# Must be set if N8N_EDITOR_BASE_URL is set, and must match the public URL.
# WEBHOOK_URL=https://your-n8n.example.com

#############################################
# Performance Settings
#############################################

# Disable diagnostics and metrics for better performance
N8N_DIAGNOSTICS_ENABLED=false
N8N_METRICS_ENABLED=false

# For a full list of available variables, refer to:
# https://docs.n8n.io/hosting/configuration/environment-variables/
